###############################################################################
# Environment variable reference.
#
# Copy to `.env.local` for local development:
#     cp .env.example .env.local
#
# `.env.local` is gitignored. This file is the tracked contract: every variable
# the application reads MUST be documented here and declared in `lib/env.ts`.
# Nothing in this file may contain a real secret.
#
# Where each variable comes from in production:
#   - PORT / HOSTNAME .................. injected by Cloud Run
#   - NEXT_PUBLIC_* .................... baked in at build time by the Docker
#                                        build args in .github/workflows/deploy.yml
#   - everything else .................. set as Cloud Run env vars or, for
#                                        secrets, mounted from Secret Manager
###############################################################################

# -----------------------------------------------------------------------------
# Runtime
# -----------------------------------------------------------------------------

# Node runtime mode. Set automatically by `next dev` / `next build` / Docker.
# Do not set this by hand in .env.local.
# Values: development | production | test
NODE_ENV=development

# TCP port the server binds to.
# Cloud Run injects this (8080 by default) and the container MUST honour it —
# a hardcoded port is the #1 cause of "container failed to start" deploys.
# Default: 3000 locally, 8080 in the container.
PORT=3000

# Network interface to bind. Must be 0.0.0.0 inside a container, otherwise the
# process is unreachable from outside it. Set to 0.0.0.0 in the Dockerfile.
# Default: 0.0.0.0
HOSTNAME=0.0.0.0

# Minimum severity that gets logged. Anything below is dropped.
# Values: debug | info | warn | error
# Default: info. Use `debug` locally, keep `info` in production.
LOG_LEVEL=debug

# -----------------------------------------------------------------------------
# Application — public (INLINED INTO THE CLIENT BUNDLE)
#
# WARNING: every NEXT_PUBLIC_* value is embedded in the JavaScript served to
# browsers and is therefore public. Never put a credential behind this prefix.
# These are read at BUILD time, so changing them requires a rebuild, not just a
# Cloud Run env var update.
# -----------------------------------------------------------------------------

# Canonical public origin. Used for metadataBase, canonical URLs, OG images.
# Optional. Cloud Run generates the service URL only after the first deploy, so
# this is not a startup requirement — the app falls back to the value below.
# In CI it is set to the Cloud Run service URL and inlined on the next build.
# Example: https://my-app-abc123-as.a.run.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Display name shown in page titles and structured logs.
NEXT_PUBLIC_APP_NAME=Next.js on Cloud Run

# Build identifier. CI sets this to the commit SHA so /api/health reports
# exactly which revision is serving traffic.
# Default: dev
NEXT_PUBLIC_APP_VERSION=dev

# -----------------------------------------------------------------------------
# Google Cloud
#
# Not required to run the app locally. Cloud Run populates the identity it needs
# via the attached service account and the metadata server — never ship a key.
# -----------------------------------------------------------------------------

# GCP project that owns Artifact Registry and the Cloud Run service.
# Also configured as the GCP_PROJECT_ID GitHub Actions variable.
GCP_PROJECT_ID=

# Deployment region, e.g. asia-south1, europe-west1, us-central1.
# Must match the Artifact Registry repository's region to avoid cross-region
# image pulls (slower cold starts and egress cost), AND the region of the
# Firestore database and the bucket below. scripts/gcp-bootstrap.sh creates all
# three in one region for exactly this reason.
GCP_REGION=asia-south1

# Set automatically by the deploy workflow (UTC, ISO-8601). Leave empty locally.
# /api/health renders it in IST so you can confirm a deploy without opening GitHub.
DEPLOYED_AT=

# -----------------------------------------------------------------------------
# Data layer — Firestore and Cloud Storage
#
# Required in production. The container refuses to start without them, so a
# misconfigured revision fails fast and Cloud Run keeps serving the previous
# one. They are optional locally and in tests, so a clean checkout runs with no
# Google Cloud account at all.
#
# All four are created and printed by scripts/gcp-bootstrap.sh.
# -----------------------------------------------------------------------------

# Machine name of this app. One slug names the Cloud Run service, the runtime
# service account, the Firestore database and the bucket, so two apps in one
# GCP project can never reach each other's data.
# Set by scripts/rename-project.sh. Lowercase letters, digits and hyphens.
APP_SLUG=

# Firestore database this app reads and writes. A NAMED database, never
# "(default)": the runtime service account's access is granted under an IAM
# condition pinned to this exact name.
# Defaults to "<APP_SLUG>-db" when left empty.
#
# ⚠ A database's location is permanent. It cannot be changed or moved.
FIRESTORE_DATABASE_ID=

# Cloud Storage bucket for user uploads.
# Defaults to "<GCP_PROJECT_ID>-<APP_SLUG>-media" when left empty.
#
# LOCALLY, point this at the dev bucket that bootstrap creates — "<bucket>-dev"
# — so a laptop never writes into production storage.
GCS_BUCKET=

# Set ONLY by the emulator runner (scripts/run-emulator-tests.sh) or by hand
# for local development against `pnpm db:emulator`. When present, the Firestore
# SDK talks to the emulator and sends no credentials.
#
# ⚠ Never set this in a deployed environment. It would silently route every
#   read and write to a host that does not exist.
# FIRESTORE_EMULATOR_HOST=127.0.0.1:8085

# -----------------------------------------------------------------------------
# Authentication — Firebase Auth (Google sign-in and phone OTP)
#
# Get these from the Firebase console:
#   Project settings > General > Your apps > Web app > SDK setup and configuration
#
# ⚠ Every value here is NEXT_PUBLIC_, so it is inlined into the browser bundle
#   at BUILD time. Two consequences:
#     1. Changing one on the Cloud Run service does nothing. Rebuild the image.
#     2. Every value is public. That is fine — none of them grants access.
#
# The API key is NOT a secret. The browser must send it to reach Identity
# Platform, so it ships in the bundle by design. It names the project; it
# authorises nothing. Restrict it by HTTP referrer in the Google Cloud console
# (APIs & Services > Credentials) rather than trying to hide it.
#
# Leave these empty and sign-in is disabled. The app still boots and still
# serves public reads, but every write route answers 401, because nobody can
# hold a session. That is the intended fail-closed default.
# -----------------------------------------------------------------------------

NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Defaults to "<NEXT_PUBLIC_FIREBASE_PROJECT_ID>.firebaseapp.com". Set it
# explicitly only when you serve auth from a custom domain.
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=

# Defaults to GCP_PROJECT_ID. A Firebase project IS a GCP project, so set this
# only when authentication lives in a different project from the data.
NEXT_PUBLIC_FIREBASE_PROJECT_ID=

# How long a session cookie lasts, in days. Firebase caps this at 14, so a
# larger value is rejected at startup rather than clamped. Default: 14
AUTH_SESSION_MAX_AGE_DAYS=14

# Check every request against revoked refresh tokens.
#
# Off by default. On, "sign out everywhere" takes effect immediately, at the
# cost of one call to Identity Platform on EVERY authenticated request — a
# per-request dependency on an external service in the hot path. Turn it on for
# an app handling money or health data. Values: true | false
AUTH_CHECK_REVOKED=false

# -----------------------------------------------------------------------------
# Health checks
# -----------------------------------------------------------------------------

# Enables the dependency checks on GET /api/health?deep=1 (one Firestore key
# lookup, one bucket metadata read).
#
# Off by default, and it should usually stay off. Deep mode answers 503 when a
# dependency is down — right for an uptime check, catastrophic for a Cloud Run
# liveness probe, which would kill healthy containers during a Firestore blip.
# Point only dashboards at it. Values: true | false
HEALTH_DEEP_CHECKS_ENABLED=false

# -----------------------------------------------------------------------------
# Secrets — examples only, uncomment and adapt per project
#
# Never store real secrets in any .env file that is committed. In production,
# create them in Secret Manager and reference them from Cloud Run:
#
#   gcloud secrets create DATABASE_URL --replication-policy=automatic
#   printf '%s' "$VALUE" | gcloud secrets versions add DATABASE_URL --data-file=-
#   gcloud run services update SERVICE --set-secrets=DATABASE_URL=DATABASE_URL:latest
#
# See cloud/environment-variables.md for the full procedure.
# -----------------------------------------------------------------------------

# DATABASE_URL=postgresql://user:password@host:5432/dbname
# API_KEY=
# NEXTAUTH_SECRET=
